# Soundwaves Database

This directory contains the local PostgreSQL development environment for the Soundwaves project.

## Technology

- PostgreSQL 18.6
- Docker Compose
- UTF-8 encoding
- UTC timezone

## Quick Start

From the `database/` directory:

```bash
cp .env.example .env
docker compose up -d
